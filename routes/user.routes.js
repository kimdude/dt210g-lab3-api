/* Routes for handling users */
const controller = require("../controllers/user.controller");

module.exports = (server) => {
    server.route([

        //Adding user

        //Loging in user

    ])
}

